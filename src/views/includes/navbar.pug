nav.navbar.navbar-inverse
  .container-fluid
    .navbar-header
      a(class='navbar-brand' href='#') Jump and Run
    ul.nav.navbar-nav
      - var pages = ['Home', 'Game', 'Controls', 'About']
      each page in pages
        li
          a(href= page.toLowerCase())= page